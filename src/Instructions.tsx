import React from 'react';

const Instructions = () => {
  return (
    <div
      style={{
        border: '1px solid white',
        padding: '0.5em',
        marginTop: '-2rem',
      }}
    >
      Start by indicating the type of chart you want followed by the colour in
      the child block, e.g. "bar white" or "line pink" or "pie #ffffff" or "area
      rgb(250,100,80)". Refer to the Readme for more instructions!
    </div>
  );
};

export default Instructions;
